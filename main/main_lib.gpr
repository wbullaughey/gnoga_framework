--with "aunit";
with "base";
with "framework";
with "gnoga";
with "root";

library project Main_Lib is

   for Languages use ("Ada");
   for Object_Dir use Root.Object_Directory;
   for Library_Name use "main_lib";
   for Library_Kind use Base.Library_Kind;
   for Library_Dir use Root.Library_Directory;
   for Source_Dirs use (".");
   for Source_Files use (
      "Top_Window.adb",
      "Top_Window.ads"
   );

   package Compiler renames Root.Compiler;

end Main_Lib;